{
  "buildCommand": "npm run build",
  "installCommand": "(npm ci) || (sleep 10 && npm ci) || (sleep 30 && npm ci)",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
